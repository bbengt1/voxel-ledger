# `backend/scripts/`

Migration tooling lives at the repo-root [`scripts/`](../../scripts) so
the conftest path-injection picks it up cleanly. See
[`scripts/v1_migration/`](../../scripts/v1_migration) for the v1 → v2
data migration scaffolding.
